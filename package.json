{
  "name": "@leaa/monorepo",
  "author": "solidzoro@live.com",
  "version": "0.0.2",
  "private": true,
  "sideEffect": false,
  "license": "BSD-3-Clause-Clear",
  "engines": {
    "node": ">=10.13.0",
    "npm": ">=6.4.0"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/module-alias",
      "**/module-alias/**",
      "**/@tarojs/**"
    ]
  },
  "scripts": {
    "clear-node-moudles": "rm ./yarn.lock && rm -rf ./yarn-error.log && rm -rf ./node_modules",
    "nuke": "yarn clear-node-moudles && yarn install --force & yarn workspaces run install",
    "u": "yarn upgrade-interactive --latest",
    "tsc-check": "yarn workspaces run tsc-check",
    "install-all": "yarn workspaces run install"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "dependencies": {
    "@tarojs/taro-weapp": "^1.3.13",
    "husky": "^3.0.3",
    "lint-staged": "^9.2.1"
  },
  "devDependencies": {}
}
