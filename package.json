{
  "name": "@leaa/monorepo",
  "private": true,
  "license": "BSD 3-Clause",
  "workspaces": {
    "packages": [
      "packages/*"
    ]
  },
  "scripts": {
    "nuke-node-moudles": "rm ./yarn.lock && rm -rf ./yarn-error.log && rm -rf ./node_modules",
    "nuke": "nuke-node-moudles && yarn install --force",
    "u": "yarn upgrade-interactive --latest",
    "tsc-check": "yarn workspaces run tsc-check"
  },
  "devDependencies": {}
}
