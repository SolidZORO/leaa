// @ts-ignore
import { APP_NAME, API_HOST, GRAPHQL_ENDPOINT } from 'react-native-dotenv';

import { IDotEnv } from '@leaa/app/src/interfaces';

export const envConfig: IDotEnv = {
  APP_NAME,
  API_HOST,
  GRAPHQL_ENDPOINT,
};
