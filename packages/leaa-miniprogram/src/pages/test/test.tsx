import Taro from '@tarojs/taro';
import { View, Button, Text } from '@tarojs/components';

import style from './style.less';

interface IProps {}

export default (props: IProps) => {
  return (
    <View>
      <Text className={style['title']}>AAAAAAAAAA</Text>
    </View>
  );
};
